# 야간 자율작업 리포트 (2026-06-29 새벽)

> 사장님이 주무시는 동안 **안전하게 할 수 있는 것**을 최대한 진행했습니다. 비밀키는 화면에 출력하지 않았고
> (존재·길이만 확인), 되돌리기 어렵거나 과금이 큰 작업은 하지 않았습니다. 아래는 **검증된 사실**과 **사장님이
> 하셔야 할 일**의 정직한 정리입니다.

## ✅ 오늘 밤 검증·완료한 것 (코드는 전부 GitHub master에 푸시됨)

1. **🎉 핵심 제품 기능 실증** — 사장님 **실제 OpenAI 키**로 *합성 얼굴 → gpt-image-2 편집 → 전문 LinkedIn 헤드샷* 생성 성공. (결과 이미지 확인함: 정장·스튜디오 배경의 진짜 프로필 사진.) **제품의 핵심이 실제로 작동합니다.**
2. **🎉 모델 ID 전부 실재 확인 (최대 출시 리스크 해소)** — 무료 모델목록 조회로 검증:
   - OpenAI: `gpt-image-2`, `gpt-image-2-2026-04-21` ✅ 존재 (키 정상)
   - Google: `gemini-3-pro-image`, `gemini-3.1-flash-image-preview`, `nano-banana-pro-preview` ✅ 존재 (키 정상)
3. **Gemini 연동 적응** — 사장님 키는 **직접 Google 키**(AI Gateway 키가 아님)라서, 코드를 `@ai-sdk/google` 직접 provider로 바꿔 그 키로 동작하게 함. 실 API에 요청이 정상 도달함을 확인(인증·모델ID 통과).
4. **워터마크 한글폰트** — "AI 생성" 라벨을 폰트 없는 SVG 외곽선으로 임베드 → 리눅스/Vercel에서도 안 깨짐(법정 라벨). 렌더 확인 완료.
5. **출시 차단 '코드' 이슈 0건** — Vitest 152 · pgTAP 46 · build · tsc 전부 그린.

## 🔴 사장님만 하실 수 있는 일 (출시 전 필수)

| # | 할 일 | 왜 / 메모 |
|---|---|---|
| A | **Google AI 결제(유료 티어) 활성화** | 사장님 Gemini 키가 **무료 티어라 쿼터 초과**로 이미지 생성이 막힘. (OpenAI는 정상.) Google AI Studio/Cloud에서 결제 등록하면 Gemini도 바로 동작. **모델·코드는 이미 맞음.** |
| B | **ProfAI 전용 프로덕션 Supabase 프로젝트 생성** | 현재 이 레포에 **링크된 프로젝트가 없습니다**. 계정에 보이는 "카달로그(Readdy)"·기본 프로젝트는 ProfAI용이 아님. 새 프로젝트 만들고 알려주시면 → 제가 `db push`로 마이그레이션(0006 포함) 적용. |
| C | **Vercel에 GitHub 레포 연결(Import)** | 이 레포에 Vercel 링크가 없고 CLI도 미설치라 제가 직접 배포 불가. Vercel에서 `yun313-star/profile_gen` 임포트해주시면 → env 입력 안내 + 배포. |
| D | **Kakao / Google OAuth 키** | (말씀하신 대로 추후) → Supabase Auth에 등록 |
| E | **PayApp linkkey / linkval** | (추후) `설정 → 연동정보`에서 확인 → env 업데이트 |
| F | **BUSINESS_INFO 실값** | 상호·대표·사업자번호·통신판매신고번호·주소·연락처 → 알려주시면 제가 채워 `test:prelaunch` 통과시킴 |
| G | **약관·개인정보처리방침 법률검토** | 얼굴=민감정보 분류·청약철회 문구 (legal-counsel 초안 가능) |

## 다음에 제가 바로 할 일 (사장님이 위를 주시는 즉시)
- B 주시면 → 프로덕션 Supabase 마이그레이션 배포 + Auth provider 설정 안내
- C 주시면 → Vercel env 항목별 입력 안내 + 배포
- F 주시면 → BUSINESS_INFO 채우고 커밋
- A 하시면 → Gemini 실생성 재검증(현재 쿼터로 막힘) + `image`→`file` 파트 deprecation 정리

## 후속 코드 과제 (낮은 우선순위, 기록만)
- `lib/models/gemini.ts`: AI SDK가 `image` 콘텐츠 파트를 deprecated → `file` 파트로 마이그레이션. **현재 포맷도 동작**하므로(에러는 쿼터 때문이었음) 급하지 않음. Gemini 결제 활성화 후 재검증하며 교체 권장.
- 실 Supabase 대상 통합 스모크(생성/결제/삭제 전 경로) — 오늘 service_role 권한 버그를 이 방식으로 잡았듯, 프로덕션 배포 후 한 번 더 권장.

---
**요약**: 제품의 심장(이미지 생성)이 실제 키로 **작동함을 증명**했고, 모델 ID 리스크를 없앴으며, Gemini 연동을 사장님 키에 맞췄습니다. 남은 건 **계정·결제·인프라 연결**(대부분 사장님 권한) — 위 A·B·C부터 주시면 배포까지 제가 끌고 가겠습니다. 코드 상태: master `848bb1f`, GitHub 동기화 완료.
